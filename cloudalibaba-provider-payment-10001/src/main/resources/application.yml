server:
  port: 10001

spring:
  application:
    name: cloud-alibaba-payment-service

#nacos注册地址
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

#监控点
management:
  endpoints:
    web:
      exposure:
        include: '*'