server:
  port: 10001

spring:
  application:
    name: cloud-alibaba-payment-service

  cloud:
    nacos:
      #nacos注册中心地址
      discovery:
        server-addr: localhost:8848
      #nacos注册配置中心地址
      config:
        server-addr: localhost:8848
        #指定配置文件类型
        file-extension: yaml
