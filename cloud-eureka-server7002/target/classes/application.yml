server:
  port: 7002

eureka:
  instance:
    hostname: eureka7002.com  #服务实例名称
  client:
    fetch-registry: false
    register-with-eureka: false # 是否要将自己注册到注册中心

    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
